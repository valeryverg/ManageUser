{% extends '::layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
